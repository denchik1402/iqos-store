# iqos-store.ru — исправление Яндекс.Вебмастера (DNS, Метрика, регион)

Инструкция после диагностики Вебмастера (июнь 2026).  
**Сервер:** `178.253.44.52` · **Домен:** `iqos-store.ru` · **Папка:** `/home/lilstore/iqos-store`

---

## Краткий итог

| Проблема в Вебмастере | Статус сейчас | Что делать |
|----------------------|---------------|------------|
| Ошибка DNS с 30.06.2026 | Снаружи DNS и HTTPS **работают** (A → 178.253.44.52) | Проверить делегирование NS + нажать «Проверить снова» в Вебмастере |
| Не включён обход по счётчикам | Настройка только в интерфейсе Яндекса | Включить в Вебмастере + проверить `YANDEX_METRIKA_ID` на сервере |
| Регион сайта | Не задан | Указать **Москва** в настройках Вебмастера |
| Яндекс Бизнес | Рекомендация | Добавить организацию вручную (по желанию) |

---

## Часть 1. Ошибка DNS (самое важное)

### Почему Яндекс пишет «ошибка DNS»

Чаще всего это **не баг сайта**, а одно из:

1. **30.06.2026** вы перенесли DNS на Selectel — несколько часов (иногда сутки) резолверы Яндекса видели старые/пустые записи.
2. У **регистратора домена** (где куплен `iqos-store.ru`) указаны **не все** NS-серверы Selectel.
3. На сервере были закрыты порты **80/443** (firewall хостера или ufw).
4. nginx / gunicorn были недоступны в момент проверки робота.

Сейчас с обычных DNS `iqos-store.ru` → `178.253.44.52`, сайт отдаёт **HTTP 200**.

### 1.1. Проверка DNS с вашего ПК (PowerShell)

```powershell
nslookup iqos-store.ru
nslookup www.iqos-store.ru
nslookup -type=NS iqos-store.ru
```

**Должно быть:**

- `iqos-store.ru` → **Address: 178.253.44.52**
- `www.iqos-store.ru` → **178.253.44.52**
- NS (все четыре):
  - `a.ns.selectel.ru`
  - `b.ns.selectel.ru`
  - `c.ns.selectel.ru`
  - `d.ns.selectel.ru`

Если NS только один (`a.ns.selectel.ru`) — исправьте у **регистратора** (см. 1.2).

### 1.2. DNS-записи в Selectel (у вас уже есть)

В панели Selectel → **Домены** → `iqos-store.ru` → **DNS-записи**:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | `@` | `178.253.44.52` | 300–3600 |
| A | `www` | `178.253.44.52` | 300–3600 |
| TXT | `@` | `yandex-verification: f5f465a5a59bd8da` | 3600 |

**Не удаляйте** TXT — по нему подтверждён Вебмастер.

### 1.3. Делегирование NS у регистратора (частая причина ошибки)

В Selectel вы видите записи A/TXT, но **кто отвечает за NS** — решает **регистратор** (Reg.ru, RU-CENTER, Selectel как регистратор и т.д.).

1. Войдите туда, где **куплен** домен `iqos-store.ru`.
2. Раздел **DNS-серверы** / **Делегирование** / **NS-серверы**.
3. Укажите **все 4** сервера Selectel:

```
a.ns.selectel.ru
b.ns.selectel.ru
c.ns.selectel.ru
d.ns.selectel.ru
```

4. Сохраните. Подождите **2–24 часа**.

### 1.4. Проверка доступности сайта

```powershell
curl.exe -I --connect-timeout 15 https://iqos-store.ru/
curl.exe -I --connect-timeout 15 https://iqos-store.ru/robots.txt
curl.exe -I --connect-timeout 15 https://iqos-store.ru/sitemap.xml
```

Все три должны вернуть **HTTP/1.1 200** (или 301 только для http→https, затем 200).

### 1.5. Починка на сервере (SSH)

```bash
ssh root@178.253.44.52
cd /home/lilstore/iqos-store
bash deploy/ensure_public_web.sh
```

Скрипт: открывает **ufw** (22, 80, 443), проверяет **SITE_URL**, nginx, SSL, healthcheck.

Проверка служб:

```bash
systemctl status iqos-store nginx --no-pager
systemctl status iqos-store-healthcheck.timer --no-pager
curl -sI http://127.0.0.1:8000/health
```

### 1.6. FirstByte / firewall хостера

Если с ПК `Test-NetConnection 178.253.44.52 -Port 443` → **False**, а на сервере nginx работает:

1. Панель хостера FirstByte → **Firewall** / **Security groups**.
2. Разрешить входящие **TCP 80, 443** с `0.0.0.0/0`.
3. Повторить проверку curl.

### 1.7. Сброс ошибки в Яндекс.Вебмастере

1. [https://webmaster.yandex.ru](https://webmaster.yandex.ru) → сайт **iqos-store.ru**.
2. **Диагностика** → блок **Ошибки** → «Не удалось подключиться… DNS».
3. Нажмите **Проверить снова** / **Я исправил** (если есть).
4. **Индексирование** → **Проверка ответа сервера** — введите `https://iqos-store.ru/` → должно быть **200 OK**.

Ошибка может висеть **несколько дней** после исправления — это нормально, если сайт уже открывается.

---

## Часть 2. Обход по счётчикам Метрики

Вебмастер пишет: счётчик привязан, но **обход не включён**.

### 2.1. Отдельный счётчик для iqos-store.ru

**Нельзя** использовать счётчик lilstore.ru (`109480691`).

1. [https://metrika.yandex.ru](https://metrika.yandex.ru) → **Добавить счётчик**.
2. Имя: `iqos-store.ru`
3. Адрес: `https://iqos-store.ru`
4. Создать → скопировать **номер** (только цифры).

### 2.2. Прописать ID на сервере

```bash
ssh root@178.253.44.52
nano /home/lilstore/iqos-store/config.py
```

```python
SITE_URL = 'https://iqos-store.ru'
YANDEX_METRIKA_ID = 'ВАШ_НОВЫЙ_НОМЕР'   # не 109480691
```

```bash
systemctl restart iqos-store
```

### 2.3. Убедиться, что код счётчика на сайте

1. Откройте https://iqos-store.ru/
2. **Ctrl+U** (исходный код) → найдите `mc.yandex.ru/metrika` или `ym(НОМЕР`.
3. Если нет — проверьте `YANDEX_METRIKA_ID` и перезапуск службы.

### 2.4. Включить обход в Вебмастере

1. [https://webmaster.yandex.ru](https://webmaster.yandex.ru) → **iqos-store.ru**.
2. **Настройки** → **Связь с Яндекс.Метрикой** (или **Индексирование** → **Обход по счётчикам**).
3. Выберите счётчик **iqos-store.ru**.
4. Включите **«Использовать данные Метрики для обхода сайта»** / **«Включить обход»**.
5. Сохранить.

Через 1–3 дня в разделе обхода появятся посещённые роботом URL.

### 2.5. Sitemap (если ещё не добавлен)

**Индексирование** → **Файлы Sitemap** → добавить:

```
https://iqos-store.ru/sitemap.xml
```

### 2.6. Переобход вручную (ускорение)

**Индексирование** → **Переобход страниц** — добавить по очереди:

- `https://iqos-store.ru/`
- `https://iqos-store.ru/catalog`
- `https://iqos-store.ru/blog`
- `https://iqos-store.ru/prices`

Лимит запросов в день ограничен.

---

## Часть 3. Регион сайта

1. Вебмастер → **iqos-store.ru** → **Настройки** → **Региональность** / **Регион сайта**.
2. Выберите **Россия → Москва** (магазин ориентирован на Москву и доставку по РФ).
3. Сохранить.

Если продажи по всей России без привязки к городу — можно **«Нет региона»**, но для локального магазина лучше **Москва**.

---

## Часть 4. Яндекс Бизнес (рекомендация)

Не влияет на DNS, но даёт расширенный сниппет и Карты.

1. [https://business.yandex.ru](https://business.yandex.ru) → **Добавить организацию**.
2. Название, адрес: **Москва, Ленинградское шоссе, 16А** (как в `config.py` → `SITE_ADDRESS`).
3. Телефон: **+7 (993) 596-82-25**.
4. Сайт: **https://iqos-store.ru**
5. Категория: розничная торговля / табачные изделия (по правилам площадки).
6. Загрузите фото, часы работы, подтвердите владение (звонок/письмо).

Свяжите с тем же аккаунтом Яндекса, что и Вебмастер.

---

## Часть 5. config.py на проде (чеклист)

На сервере `/home/lilstore/iqos-store/config.py`:

```python
SITE_URL = 'https://iqos-store.ru'
SITE_CITY = 'Москва'
SITE_ADDRESS = 'Москва, Ленинградское шоссе, 16А'
SITE_PHONE = '+7 (993) 596-82-25'
YANDEX_METRIKA_ID = 'XXXXXXXX'   # свой счётчик iqos-store.ru
TELEGRAM_RUN_POLLING = False     # бот только на lilstore.ru
```

Проверка:

```bash
cd /home/lilstore/iqos-store && source venv/bin/activate
python3 audit_site.py
python3 audit_live.py
```

---

## Часть 6. Автомониторинг (после следующего деплоя)

В репозитории добавлены:

- `deploy/healthcheck.sh` — проверка `/`, `robots.txt`, `sitemap.xml`, `/health`
- `deploy/iqos-store-healthcheck.timer` — каждую минуту на сервере

После `git push` в `main` таймер установится автоматически. Вручную:

```bash
cp /home/lilstore/iqos-store/deploy/iqos-store-healthcheck.* /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now iqos-store-healthcheck.timer
```

---

## Часть 7. Порядок действий (по шагам)

1. **PowerShell:** `nslookup` + `curl` (часть 1.1, 1.4).
2. **Регистратор:** 4 NS Selectel (часть 1.3).
3. **SSH:** `bash deploy/ensure_public_web.sh` (часть 1.5).
4. **FirstByte:** порты 80/443 (часть 1.6).
5. **Метрика:** новый счётчик + `config.py` (часть 2).
6. **Вебмастер:** обход по счётчикам + sitemap + «Проверить снова» по DNS (части 1.7, 2.4, 2.5).
7. **Вебмастер:** регион Москва (часть 3).
8. **По желанию:** Яндекс Бизнес (часть 4).
9. Через 3–7 дней: в поиске `site:iqos-store.ru`.

---

## Если ошибка DNS не исчезает через 5 дней

Напишите в поддержку Яндекс.Вебмастера (кнопка «Обратная связь») и приложите:

- скрин `nslookup iqos-store.ru` с IP `178.253.44.52`;
- скрин `curl -I https://iqos-store.ru/robots.txt` с ответом 200;
- дату переноса DNS на Selectel (30.06.2026).

Полная инструкция по деплою двух сайтов: `../DEPLOY_TWO_SITES.md` (части 1, 9, 12–14).
